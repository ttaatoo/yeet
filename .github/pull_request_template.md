## Summary

## Changes

## Testing

- [ ] Built and ran the app (`yeet.xcodeproj` / scheme `yeet`), and exercised the change
- [ ] `web/` or `scripts/` touched → `bun run typecheck` in `web/`

## Checklist

- [ ] Did not bump `MARKETING_VERSION` or `CURRENT_PROJECT_VERSION`
- [ ] User-visible product change → `CHANGELOG.md` `[Unreleased]` (final outcome only)
- [ ] New UI is AppKit, not new SwiftUI
